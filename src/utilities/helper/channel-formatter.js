export const notificationChannel = (uid) => {
  return `notification-${uid}`;
};

export const announcementChannel = (uid) => {
  return `announcement-${uid}`;
};

export const locationChannel = (uid) => {
  return `location-s`;
};
export const groupLocationChannel = (uid) => {
  return `group-location-s`;
};